# V1.5 HANDOFF — Solana Agentic Trading Research Platform

Surgical improvement pass on audited V1. **LIVE trading remains disabled.** This is not a rebuild.

## STATUS

**V1.5 PAPER/DEMO research platform — audit architecture preserved, not a live trading system.**

Postgres adapter is implemented and was **successfully round-tripped against local PostgreSQL 16** in this environment. Default `pnpm --filter @sat/web run dev` without `DATABASE_URL` still uses labeled **memory**. Demo OHLCV used for historical signals is **simulated DEMO history**, not Birdeye live bars (no Birdeye key in this environment).

## SOURCE BRANCH/COMMIT

| Item | Value |
|------|--------|
| Branch | `cursor/audit-safety-gates-d1d1` |
| Verified audit code tip | `6bc79f6f002cda890b5114b3c848fcf93375e8ce` |
| V1.5 implementation | commits on top of that tip (docs + this pass). Re-read `git log` at resume. |
| Lineage | `main` (`9fff0f0`) → QA (`f7e5bec`…`6759271`) → audit (`6bc79f6`) → docs → V1.5 |
| PR | **Not created** (stay on this branch as specified) |

## WHAT CHANGED

- **P0 tests first** for live-gate, `canBroadcast` literal, no-broadcast scan, independent policy/token-risk/portfolio-risk, execute-time gates
- **P1** `PostgresDatabase` (`pg` Pool) when `DATABASE_URL` is set; `InMemoryDatabase` fallback; health `persistence: memory \| postgres`
- **P2** Typed OHLCV, Birdeye v3 then legacy `/defi/ohlcv`, historical momentum/volume/vol/RS/breakout; removed hardcoded +2% RS
- **P3** Token-risk v1.5: `token_info` authorities, `mint_extensions`, top-5/10 via `getTokenLargestAccounts` + `getTokenSupply`
- **P4** Playwright one critical DEMO/PAPER flow in CI
- **P5** Default bind `127.0.0.1`; CSRF/origin on POST; real ESLint across packages; gitleaks in CI; pattern scan includes markdown
- **P6** Experiment metrics hidden on short history; no sine-wave equity

## WHAT ACTUALLY WORKS

- Full DEMO research cycle and paper fill with `canBroadcast: false`
- SCAMX rejected by policy + token-risk + portfolio risk independently
- Duplicate paper execute blocked; `READ_ONLY` blocks execute
- Helius unknown/failure → UNKNOWN/null, not fake-clean
- Dashboard + `/api/state` on loopback
- Postgres write/read/upsert/restart restore **when Postgres is reachable**

## DATA PROVIDERS

| Provider | Invented? | This environment |
|----------|-----------|------------------|
| Jupiter lite `/swap/v1/quote` | No | Quote-only; may live-quote or demo-fallback |
| Birdeye trending/overview/OHLCV v3 | No (official docs) | No API key → DEMO market + deterministic DEMO bars |
| Helius `getAsset` + `getTokenLargestAccounts` + `getTokenSupply` | No | No API key → DEMO on-chain |
| OpenAI chat completions | No | Mock research |
| DEMO adapters | Labeled | Default path |

## PERSISTENCE STATUS

- **Adapter:** implemented (`packages/database`)
- **Without `DATABASE_URL`:** `InMemoryDatabase`, `health.persistence = "memory"`
- **With `DATABASE_URL`:** `PostgresDatabase`, JSONB `sat_*` tables, schema applied on first connect
- **Verified here:** PostgreSQL 16.15 local (`postgres://sat@127.0.0.1:5432/sat_test`) — write/read, upsert-by-proposal-id, close pool / new instance restore. Test file `tests/persistence.test.ts`
- **Not verified:** hosted Supabase project, connection pooler, SSL-to-cloud
- Do **not** claim persistence is active in a process unless health reports `postgres`

## HISTORICAL DATA STATUS

- Schema: `OhlcvBar` Zod-validated (`timestamp` ms, OHLCV)
- Intervals: 1m, 5m, 15m, 1h, 4h (Birdeye `1H`/`4H`)
- Min history: [docs/signals.md](./signals.md)
- Missing bars → `INSUFFICIENT_DATA` / low confidence; no interpolation
- RS vs SOL from aligned bars; optional BTC; **no +2% constant**
- DEMO `buildDemoOhlcv` is deterministic simulated history (labeled)

## TOKEN-RISK STATUS

- Config version `token-risk-v1.5`
- Authorities: `token_info.mint_authority` / `freeze_authority`
- Token-2022: `mint_extensions` when present; missing object → nulls
- Holders: top-5/10 when RPC succeeds; else null
- Never `SAFE`
- Fixtures: benign, mint, freeze, concentrated, permanent delegate, transfer-hook, incomplete

## TEST RESULTS

Measured in this pass (re-run before quoting later):

| Check | Result |
|-------|--------|
| `pnpm exec vitest run` | **8 files, 66 tests passed** |
| `pnpm run typecheck` | **pass** |
| `pnpm lint` | **pass** (packages + worker + tests + `@sat/web`, `--max-warnings 0`) |
| Postgres adapter tests | **passed** against local PG 16 |

Prior V1 snapshot was 36 tests / web-only eslint.

## E2E RESULTS

| Check | Result |
|-------|--------|
| `pnpm exec playwright test` | **1 passed** (`tests/e2e/critical-flow.spec.ts`) |
| Flow | dashboard → DEMO/PAPER + canBroadcast=false + LIVE OFF → SCAMX REJECTED → WIF paper fill → ledger/position → duplicate POST 400 → `set_mode LIVE` 400 → evil Origin 403 |

## BUILD RESULTS

| Check | Result |
|-------|--------|
| `pnpm --filter @sat/web run build` | **pass** (Next.js 16.3.4 Turbopack) |

## SECURITY RESULTS

| Check | Result |
|-------|--------|
| Default bind | `127.0.0.1:4317` (`dev` / `start`); `dev:remote-demo` is `0.0.0.0` and documented |
| CSRF/origin | Mutating POST 403 unless loopback Host or allowlisted Origin |
| `bash scripts/secret-scan.sh` | **clean** |
| `gitleaks detect --source . --verbose` | **no leaks** (git-tracked files) |
| `GET /api/state` (this pass, memory store) | `liveTradingAllowed: false`, `canBroadcast: false`, `persistence: "memory"`, SCAMX REJECTED/HIGH_RISK/REJECT |
| Paper execute WIF | `FILLED`, `canBroadcast: false`; duplicate **400 already paper-executed**; SCAMX **400**; `set_mode LIVE` **400**; evil Origin **403** |
| Broadcast implementation grep | no `sendTransaction` / wallet signing in `packages` or `apps` (`POST /swap` appears only as a “not used” plan note) |

**Dashboard left running:** [http://127.0.0.1:4317](http://127.0.0.1:4317) (loopback Next.js). `health.persistence` is `memory` because the process has no `DATABASE_URL`.

## CURRENT LIMITATIONS

See [limitations.md](./limitations.md). Highlights: DEMO default; memory without DATABASE_URL; incomplete Token-2022 when DAS omits `mint_extensions`; Jupiter v1 quote-only; unauthenticated API on loopback; DEMO OHLCV is simulated.

## RESIDUAL TECH DEBT

- Parallel paper-execute race still possible in memory (sequential API/UI covered)
- Jupiter Swap v1 vs v2
- LLM injection regex is still a short list
- Package `lint` scripts require workspace eslint on PATH; **source of truth is `pnpm lint`**
- Domain SQL in `20260325000000_init.sql` is not dual-written by the Node adapter
- No auth beyond origin check
- Playwright does not cover READ_ONLY UI (unit-tested)

## DO-NOT-CLAIM LIST

- Live trading, broadcast, wallet custody, or env unlock of `isLiveTradingAllowed()`
- Hosted Supabase is live (only local PG was verified)
- Birdeye/Helius/OpenAI are live unless keys are set **and** health providers are not demo/mock
- Token-2022 / holder risk is complete
- Experiment / DEMO OHLCV / short NAV is verified performance
- Complete Shariah / rug detection
- Invented user counts or PnL

## RESUME-READY FEATURES

- End-to-end paper research terminal with explicit DEMO labeling
- Independent safety gates with tests + Playwright
- Postgres-ready store
- Honest token-risk / historical-signal documentation

## TOP 5 NEXT STEPS

1. Independent security review before any LIVE/broadcast design (do not flip `isLiveTradingAllowed()`)
2. Point a real `DATABASE_URL` (hosted) and confirm SSL/pooler
3. Provision Birdeye + Helius and compare DAS `mint_extensions` against live Token-2022 mints
4. Jupiter Swap API v2 quotes (still no submit)
5. Optional README/demo-video polish (readiness B below)

## Readiness

| Track | Ready? |
|-------|--------|
| **A — portfolio demo deploy** | **Conditional.** Loopback DEMO/PAPER works. Remote deploy needs bind/origin config, must not expose an unauthenticated mutable API on a public network without extra auth. Postgres optional. |
| **B — README/demo-video polish** | **Yes, with care.** UI is demoable; label DEMO DATA / INSUFFICIENT HISTORY; do not show synthetic alpha. |
| **C — resume inclusion** | **Yes as a research/safety project**, using [resume-project-notes.md](./resume-project-notes.md). Not as a live trading or audited-security claim. |

---

**Demo:** [http://127.0.0.1:4317](http://127.0.0.1:4317)  
**Mode:** PAPER · LIVE broadcasting disabled
